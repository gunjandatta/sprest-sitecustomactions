// Wait for the document to be ready
window.addEventListener("load", function () {
    // Add a custom banner
    document.body.innerHTML = '<div id="myBanner">My Custom Banner</div>' + document.body.innerHTML;
});
//# sourceMappingURL=banner.js.map